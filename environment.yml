name: cooling
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - numpy>=2.0
  - scipy
  - matplotlib=3.10.9
  - pandas
  - seaborn
  - ipython
  # Retain the notebook kernel version range used by the working VS Code setup.
  - ipykernel>=6,<7
  - pytest=9.0.3
  - pip:
      - pennylane==0.44.1
      - juliacall
